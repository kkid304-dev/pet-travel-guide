# 멍냥로드 — 발표 준비 노트

> 발표/기능설명서 작성 시 그대로 가져다 쓸 수 있도록, 지금까지의 조사·개발 과정을 근거 수치와 함께 정리한 문서.
> 마지막 갱신: 2026-08-10 (branch: `develop`, PR #1~#4 전부 병합 완료)

---

## 1. 프로젝트 한 줄 정의

한국관광공사 반려동물 동반여행 API(KorPetTourService2)의 **동반 조건이 자유 텍스트이고 결측이 많다**는 문제를,
사용자의 반려견 조건(크기·무게)을 기준으로 자동 판정해 보완해주는 서비스.
**2026 관광데이터 활용 공모전 지정과제 6번** 취지와 일치.

---

## 2. 데이터로 검증한 문제 정의 (기획력 근거)

- 서울 기준 동반 가능 장소 **3,179곳** 중 쇼핑+음식점이 **97%**, 관광지는 **56곳(1.8%)**뿐
  → 필터 없는 목록은 사실상 상업시설 리스트 → **카테고리 필터가 필수 기능**임을 데이터로 확인
- 동반 조건(`acmpyPsblCpam`) **결측률 약 98%** (500곳 표본 중 491곳 빈 값) → `acmpyTypeCd`로 2단 fallback 구현
- 조건은 코드값이 아닌 **자유 텍스트** → 오타("전 견동"), 이중 공백 등 정규화 없이는 통계·판정 모두 왜곡
- kg 수치가 명시된 곳은 드묾 → **무게 파싱만으로는 부족, 단어 기반 해석이 반드시 필요**함을 데이터로 입증

> **발표 포인트**: "조건 해석·보완 서비스가 필요하다"를 주장이 아니라 결측률·표본 수치로 증명했다는 게 차별점.

---

## 3. 판정 로직(judge) 개발 히스토리

| 버전 | 핵심 로직 | 한계 |
|---|---|---|
| v1 | 무게 입력 + 단순 크기 판정 | 조건 텍스트 자체를 안 봄 |
| v2 | kg 숫자 파싱("10kg 이하") + "불가"/"전 견종" 키워드 | kg 없는 문장은 대부분 판정 불가(⚠️) |
| v3 | 크기 단어(소형/중형/대형) · kg 이하·미만 구분 · 맹견/안내견 분기 · 구역 제한 · 장비·서류 조건 반영 | 지역 확장에서 버그 2건 발견 (§6-1) |
| v4 | 계절·크기 예외 조건 인식(버그1 수정), 맹견 제외/장비 조건 구분(버그2 수정), "전견종" 정규화 | 전국 스캔에서 순서 버그 발견 (§6-2) |
| **v5 (현재)** | **맹견 여부를 사용자 입력으로 받아 확정 판정** — 특수 카테고리(안내견·품종특성·대형견제외·맹견)를 일반 규칙보다 먼저 검사하도록 순서 재정렬 | 안내견이 부가 허용조항으로만 붙는 복합문장 오판 가능성 (§9 잔여 항목) |

### 판정 순서 설계 원칙 (v5 기준)
- **미지의 문장은 ✅/❌로 임의 판정하지 않고 `⚠️ 조건 확인 필요`로 유보** — 오판보다 정직한 유보가 서비스 신뢰도에 낫다는 원칙은 v1부터 유지
- **"판단 기준이 없는 특수 카테고리는 반드시 일반 규칙보다 먼저 검사한다"** — v4→v5 과정에서 두 번 반복해서 확인된 원칙.
  안내견/품종특성/대형견제외/맹견처럼 별도 분기가 필요한 케이스가, "불가 있고 가능 없음"이나 "kg 이하" 같은 넓은 일반 규칙보다 뒤에 있으면
  그 일반 규칙이 먼저 걸려서 특수 처리 자체가 무시된다 (자세한 사례 §6-2)
- 안내견/보조견 언급은 이제 유보가 아니라 **확정 `❌`(일반 반려견 기준)**로 판정 — 이 서비스 사용자는 거의 항상 일반 반려견 보호자이므로 애매하게 유보하는 것보다 확정해주는 게 더 유용하다는 판단 (예외 케이스는 §9 참고)
- 맹견 여부는 더 이상 "견종 미입력이라 판정 불가"로 넘기지 않고, **사용자에게 직접 물어 확정 판정** (§6-3)

---

## 4. 조사 방법론 — 층화 수집 → 지역 확장

### 4-1. 표본 편향 발견과 층화 수집 전환
가나다순 앞 400곳을 수집했을 때 **396곳이 "전구역 동반가능"**으로 나오는 표본 편향 발견
(백화점 입점 매장이 목록 앞자리를 차지하는 구조) → 카테고리별로 표본을 할당하는 **층화 수집**으로 전환

### 4-2. 서울 1차 스캔 (2026-07-24)
- 관광지·숙박·음식점·쇼핑 4개 카테고리, **132곳**, API 호출 **136건**
- 필드 채움률: `acmpyPsblCpam` 55.3% · `acmpyNeedMtr` 47.7% · `etcAcmpyInfo` 45.5%
- judge v2 기준 `⚠️` **19건(14.4%)** 확보 → v3 설계의 근거 자료로 사용
- v3 적용 후 오프라인 재측정: 19건 중 9건(47%) 해소, 표본 전체 판정불가 비율 **14.4% → 7.6%**

### 4-3. 지역 확장 스캔 (2026-08-01)
"서울 표본만으로 충분한가, 지역마다 다른 문장이 나오지 않을까"라는 질문을 검증하기 위해
부산·제주·강원 **343곳** 추가 조사 (API 호출 355건)

| 지역 | 장소 수 | ✅ | ⚠️ | ❌ | ⚠️ 비율 | 고유 문장 비율 |
|---|---|---|---|---|---|---|
| 부산 | 85 | 81 | 4 | 0 | 4.7% | 11.8% |
| 제주 | 97 | 87 | 2 | 8 | 2.1% | 16.5% |
| 강원 | 161 | 147 | 8 | 6 | 5.0% | 13.7% |
| **합계** | 343 | 315 | 14 | 14 | **4.1%** | 11.8~16.5% |

**체인성 반복 재확인**: "전구역 동반가능"·"전 견종 동반 가능" 단 두 문구가 지역마다 22~67곳을 차지.
장소 수 기준 통계는 실제 정책 다양성을 과대평가하므로, **고유 문장 수를 항상 함께 봐야 한다**는 방법론적 교훈.

> **발표 포인트**: 표본 편향(가나다순) → 층화 수집 → 단일 지역 한계 → 지역 확장, 이 흐름 자체가
> "가정을 세우고, 데이터로 깨고, 설계를 고치는" 반복 검증 과정이었다는 걸 보여줄 수 있음.

### 4-4. 전국 스캔 (2026-08-10)

지역 확장 표본(343곳)으로도 부족하다고 판단해, 쇼핑을 제외한 전국 5개 카테고리를 **전수 조사**로 전환.
쇼핑(38)은 서울 400곳 표본에서 396곳이 "전구역 동반가능"으로 확인돼 조건 다양성이 없다고 판단해
의도적으로 제외 (설계 판단이지 누락이 아님 — 근거는 §4-1).

| 카테고리 | ID | 수집 | 목표 | 완료율 |
|---|---|---|---|---|
| 관광지 | 12 | 754 | 754 | 100% |
| 문화시설 | 14 | 25 | 25 | 100% |
| 레포츠 | 28 | 79 | 79 | 100% |
| 숙박 | 32 | 117 | 117 | 100% |
| 음식점 | 39 | 72 | 72 | 100% |
| **합계** | — | **1,047** | **1,047** | **100%** |

- 08-10 1차 실행: API 호출 1,011건에서 일일 한도(1,000건) 초과로 중단 → **재개(resume) 로직**을
  만들어 다음날 이어서 처리 가능하도록 구현 (이미 처리한 contentId를 raw 파일에서 읽어 건너뜀)
- 08-11 한도 리셋 후 재실행: 음식점 잔여 47곳을 API 호출 57건으로 완료, **전국 5개 카테고리 전수 조사 완료**
- 스캔 도중 조건 텍스트에 개행이 섞여 **레코드 하나가 파일에서 여러 줄로 깨지는 버그**를 발견 →
  저장 전 공백으로 정리(flatten)하도록 수정. 방치했으면 재개 로직이 이 레코드들을 못 읽고 중복 호출했을 것.

---

## 5. 도구화 — 재현 가능한 측정

- `scan_conditions.py` / `scan_conditions_regions.py`: API 호출 → 조건 텍스트 수집 → judge 판정 → 파일 저장까지 자동화
- `measure_judge_v3.py`: **API 호출 없이 오프라인으로** v2 대비 v3의 개선폭을 수치로 측정 (회복률 지표)
  - 측정 방식의 한계도 인지: "⚠️를 벗어났는가"만 셀 뿐 판정 자체의 정확성(정답 라벨)은 검증하지 않음 → 회귀 여부는 지역 확장 스캔으로 사실상 보완됨

---

## 6. 스캔 확장이 실제로 잡아낸 버그들 (방법론의 성과)

서울 표본만으로는 절대 드러나지 않았을 결함들이, 조사 범위를 넓힐 때마다 하나씩 나왔다.
**"모르면 유보한다"는 원칙(§3)조차 스스로 검증해서 예외를 찾아냈다는 것 자체가 이 프로젝트의 방법론적 핵심.**

### 6-1. 지역 확장(부산·제주·강원)에서 발견 — 텍스트 해석 버그 2건

**버그 1 (Critical) — 크기 단어 매칭이 예외·계절 조건을 못 읽음**
- 강원 실제 조건문: "대형견은 여름에만 동반 가능합니다... 고양이나 다른 반려동물도 입장 가능하나, 필요한 경우 케이지를 이용해야 합니다."
- 실제 의미: 사실상 다 받아주는 관대한 정책 (대형견만 계절 제한)
- v3의 실제 출력(10kg 소형견 기준): `❌ 동반 불가 (대형만 허용)` — **완전히 반대로 응답**
- 다른 ⚠️ 케이스들과 달리 **유보가 아니라 확신에 찬 오답**이라는 점에서 가장 심각한 발견. v4 최우선 수정 대상이었음.

**버그 2 (Minor) — "맹견" 언급을 전부 ⚠️로만 처리**
- 부산 실제 조건문(README 최초 예시와 동일한 문장): "전 견종 출입 가능 (맹견의 경우, 입마개 착용 필수)"
- 실제 의미: 전 견종 이미 허용, 맹견은 준비물(입마개)만 추가 조건
- v3의 실제 출력: `⚠️ 맹견 해당 여부 확인 필요` — 사실은 `✅`여야 하는 케이스를 과도하게 유보

부수 발견: "전견종"(공백 없음)이 "전 견동"(오타)처럼 정규화되지 않아 미해석.

### 6-2. 전국 스캔·코드 리뷰에서 발견 — "검사 순서" 버그 2쌍

지역 확장이 **텍스트를 어떻게 해석하는지**의 버그를 찾았다면, 전국 스캔 규모(1,000곳)와 코드 리뷰는
**여러 조건이 한 문장에 같이 있을 때 뭘 먼저 봐야 하는지**의 버그를 찾아냈다. 판정 함수는 위에서부터
순서대로 검사하다 처음 걸리는 조건에서 바로 끝나는 구조라, 순서가 틀리면 뒤에 있는 정확한 로직이
아예 실행되지 않고 앞의 엉성한 규칙이 대신 답해버린다.

**순서 버그 A — 무게 조건이 맹견 조건보다 먼저 걸림**
- 실제 조건문 12건에서 발견 (예: "맹견 제외 15kg 이하 동반 가능")
- 기존 순서: 무게 검사 → 맹견 검사. "15kg 이하"만 보고 먼저 확정해버려 "맹견 제외"라는 핵심 조건을 건너뜀
- 결과: 맹견인데 몸무게만 기준 이하면 확신에 찬 `✅`가 나가는 실질적 오판
- 수정: 맹견 검사를 무게 검사보다 앞으로 이동

**순서 버그 B — 일반 "불가" 규칙이 특수 카테고리(맹견/품종특성/대형견제외)보다 먼저 걸림**
- 자가 코드 검증 중 발견. "불가"라는 단어가 있고 "가능"이 없으면 무조건 `❌`로 판정하는 넓은 규칙이,
  실제로는 **특정 대상에만 한정된** "불가"까지 전부 삼켜버리는 문제
- 실제 조건문 3건:
  - 태화강 국가정원: "맹견 동반 불가" → 실제로는 맹견만 배제, 일반 반려견은 가능. 그런데 넓은 규칙이 먼저 걸려서 **일반 반려견까지 전부 `❌`**로 오판
  - 통인 1939: "털날림 많은 종은 불가" → 전용 메시지(`⚠️ 품종 특성 확인 필요`) 대신 일반 `❌`로 처리됨
  - 메이즈랜드: "대형견 제외 15kg 미만 입장 가능... 시각장애인 안내견 입장 가능" → 관대한 정책인데 안내견 언급 때문에 `❌ 일반 반려견 불가`로 오판 (아래 §9 잔여 항목, 원인이 달라 별도 처리)
- 수정: 특수 카테고리(안내견/품종특성/대형견제외/맹견) 검사를 전부 일반 "불가" 규칙보다 앞으로 이동

> **발표 포인트**: 순서 버그 A와 B는 **같은 원칙의 두 번 반복**이다 — "판단 기준이 없는 특수 조건은
> 반드시 일반 규칙보다 먼저 봐야 한다." 처음엔 맹견-무게 한 쌍에서 발견했고, 그 다음 리뷰 과정에서
> 똑같은 패턴이 다른 조합(특수 카테고리 전체-일반 불가규칙)에도 있다는 걸 스스로 점검해서 찾아냈다는
> 것이 이 프로젝트의 검증 방식이 1회성이 아니라 반복 가능한 절차였음을 보여준다.

### 6-3. 맹견 질문 하나의 정량적 효과

전국 스캔 결과(1,000곳) 기준, 판정불가(⚠️) 109건 중 **52건(47.7%)이 "맹견" 언급 때문**이었다.
서비스 시작 시 "맹견(도사견·핏불 등)에 해당하나요? (예/아니오)" 질문 하나를 받는 것만으로:

- 판정불가율 **10.9% → 9.0%** (질문과 무관한 다른 개선 포함, 응답 전 기준)
- 그중 맹견 관련 ⚠️ 90건 중 **52건(58%)이 사용자가 답하는 즉시 확정 판정**으로 전환
- 서울 132곳 표본 오프라인 재측정: 판정불가 비율 **14.4%(v2) → 7.6%(v3) → 3.8%(현재, 맹견 질문 반영)**

> **발표 포인트**: "기능 하나가 서비스 품질에 미치는 영향을 숫자로 증명"한 사례. 막연히 "질문을 추가하면
> 좋을 것 같다"가 아니라, 스캔 데이터에서 ⚠️의 원인을 분해해보니 거의 절반이 한 가지 원인(맹견)에
> 몰려 있다는 걸 먼저 확인하고, 그 원인 하나를 해소하는 기능을 설계한 순서. 무게 입력이 숙박 카테고리
> 판정에 크게 기여했던 것처럼, 맹견 질문은 안내견 표기가 몰려 있던 것과 별개로 관광지·숙박 전반의
> 판정 품질을 끌어올렸다.

### 6-4. 코드 이해도 자가 검증 (개발 프로세스 노트)

judge() 로직 상당 부분을 AI 코딩 에이전트와 함께 설계·구현했기 때문에, "왜 이렇게 판정했는가"를
직접 설명하지 못하면 기능 심사·발표에서 신뢰도가 떨어질 수 있다는 문제의식으로 **자가 검증 절차**를
도입했다: 판정 순서를 종이에 재구성해보고, 실제 사례를 손으로 추적하고, 회귀 테스트 각각이 막는
케이스를 설명할 수 있는지 스스로 확인. 이 과정에서 위 순서 버그 B(6-2)를 실제로 찾아냈다 —
자가 검증이 형식적 절차가 아니라 실제로 결함을 찾아내는 데 기여했다는 근거.

### 6-5. 전국 스캔 완료(1,047곳) 후 전수 재검토 — 눈으로 훑기의 한계

전국 스캔 완료 직후 "특이 조건이 있는지" 사람이 직접 목록을 눈으로 훑어 답한 결과, 실제로 존재하던
사례 하나(음식점 "반려동물동반 가능")를 빠뜨렸다. 이 누락을 계기로 **전체 1,047건을 코드로 전수 재검사**했더니:

- 순수 미해석(⚠️) 20건을 유형별로 분류: "XX동반 가능"류 미인식 표현 10건(기존에 알려진 한계),
  "일부 견종"처럼 정보 자체가 없어 정당하게 유보되는 3건, "1인당/객실당 N마리" 마리 수 제한 4건(v3.1 후보로 이미 인지),
  기타 애매한 2건
- **새로 발견한 실제 버그 1건**: 남이섬 "20㎏ 미만 동반 가능" — `kg` 정규식이 영문자만 잡고
  **유니코드 완성형 기호 "㎏"(U+338F)를 인식 못 함**. 눈으로 볼 땐 "kg"와 구분이 안 되지만 다른 문자.
  정규식에 `㎏` 추가해 수정 (v5.1)
- 특수 카테고리 순서 버그(6-2)는 전체 재검사에서 추가 발견 없음 — §6-2 수정이 실제로 전수 해결됐음을 확인

> **발표 포인트**: "AI가 짠 코드를 사람이 눈으로 검토"하는 것도 완벽하지 않다는 걸 스스로 증명한 사례.
> 사람의 수작업 검토가 놓친 것을 코드 기반 전수 검사로 다시 잡아낸 것 — 검증 방법 자체를 한 단계
> 더 신뢰할 수 있는 방식으로 교체해나간 과정으로 설명 가능.

---

## 7. 개발 프로세스 (엔지니어링 근거)

- Git 브랜치 전략: `main` → `develop` → `feat/*` → PR 리뷰 → 머지. PR #1~#4 전부 충돌 없이 병합 완료
  - PR #1 `feat/judge-v3-condition-scan`: 서울 스캔 + judge v3 (07-31)
  - PR #2 `feat/region-expansion-scan`: API 방어 코드 공통화 + 지역 확장 스캔 (08-09)
  - PR #3 `feat/dangerous-breed-question`: 맹견 질문 도입 + 순서 버그 A 수정 (08-10)
  - PR #4 `feat/nationwide-scan-no-shopping`: 전국 스캔 + 재개 로직 (08-10)
- 커밋을 작업 단위로 분리 (스캔 결과 커밋과 로직 변경 커밋을 섞지 않음) — 로그만 봐도 무엇이 왜 바뀌었는지 추적 가능
- 측정 스크립트(`measure_judge_v3.py`)와 회귀 테스트(`test_judge.py`, 20개 케이스)로 로직 변경의 효과를 "감"이 아니라 숫자로 검증하는 습관화
- 코드 이해도 자가 검증 절차 도입 (§6-4) — AI와 함께 짠 코드라도 설계자가 직접 설명 가능한 상태를 유지하는 것을 프로세스에 명시적으로 포함

---

## 8. API 운영 노트 (안정성 근거)

- 일 호출 한도 1,000건, 자정 기준 리셋
- 한도 초과 시 JSON이 아닌 일반 텍스트("API token quota exceeded")로 응답 → `try/except` 방어 없이는 수집기가 중단됨
- 매 호출 간 0.2초 대기로 과호출 방지
- 지역 확장 스캔 당일(08-01) API 사용량 710/1,000건(71%) — 대규모 스캔 시 한도 관리가 실제 운영 이슈였음을 보여주는 사례
- 전국 스캔(08-10) 1,011건에서 한도 초과 — **하루 한도로는 완전 전수조사가 애초에 불가능한 규모**(목표 1,048곳)라는 걸
  확인하고, 재개(resume) 로직을 갖춘 스크립트로 여러 날에 걸쳐 나눠 처리하도록 설계 변경

---

## 9. 앞으로 남은 일 (우선순위)

judge 로직(v1→v5)과 관련 버그 수정은 사실상 마무리됐고, Flask 착수를 막는 항목은 더 이상 없음.
남은 건 병행 가능한 것들:

1. 음식점 잔여 47곳 스캔 (API 한도 리셋 후 `scan_nationwide_no_shopping.py` 재실행 — 재개 로직 검증 완료라 그냥 실행만 하면 됨)
2. **v3.1 후보 결정** (급하지 않음, Flask와 병행 가능):
   - 맹견+무게 AND 결합 미해결 — "맹견 제외 15kg 이하"에서 맹견 아님(False)으로 확정된 뒤 무게 조건은 재검증하지 않음 (실 데이터 12건 영향)
   - cm·나이·마리 수 등 무게 외 단위 조건 (순천만국가정원 4중 조건 사례)
   - 안내견이 부가 허용조항으로만 붙는 복합문장 오판 (메이즈랜드 사례, §6-2) — 안내견 언급 자체가 드물어 우선순위 낮음
3. (선택) 쇼핑 100곳 표본 검증 — README에 "표본 검증 후 제외" 근거 한 줄 추가용, 없어도 서비스·심사에 지장 없음
4. **Flask 웹 전환** — judge()·API 헬퍼는 이식 준비 완료. 맹견 여부 입력 UI, 카테고리 필터, 데이터 편향 안내 문구 추가 필요
5. **배포** — 08월 말 목표 (웹서비스 형태, 모바일 접속 가능한 공개 URL)
6. **기능설명서 작성** — 1차 심사(10월 중) 제출물. 지정과제 안내 자료에 명시된 해시태그
   (`#반려동물 여행` `#헛걸음 방지` `#조건 해석` `#여행 가이드`) 기준으로 기능을 매핑해 작성 요청받음 — 09월 버퍼 기간에 착수

---

## 10. 발표 시 강조하면 좋을 스토리 라인

1. "왜 이 서비스가 필요한가" → 결측률 98%, 자유 텍스트라는 사실을 데이터로 제시 (§2)
2. "어떻게 검증했는가" → 표본 편향 발견 → 층화 수집 → 서울 한계 인지 → 지역 확장 → 전국 스캔이라는 **반복적 자기 검증 과정** (§4)
3. "그래서 무엇을 찾았는가" → 텍스트 해석 버그 2건(§6-1) + 조건 순서 버그 2쌍(§6-2), 총 4건의 실제 오판 사례를 스스로 찾아 고쳤다는 구체적 성과
4. "설계 철학" → 모르면 확답하지 않고 유보한다는 원칙, 그리고 "판단 기준 없는 특수 조건은 일반 규칙보다 먼저 봐야 한다"는 원칙을 두 번의 서로 다른 버그에서 반복 확인한 일관성 (§6-2)
5. **"기능 하나의 효과를 숫자로 증명"** → 맹견 질문이라는 기능 하나가 ⚠️의 47.7%를 해소한다는 것을 스캔 데이터로 먼저 확인하고 나서 설계한 순서 (§6-3) — 감이 아니라 데이터 기반 기획이라는 근거
6. "코드를 얼마나 이해하고 있는가" → AI 도구를 활용하되, 판정 로직을 직접 설명할 수 있도록 자가 검증 절차를 거쳤고 그 과정에서 실제 버그를 찾았다는 정직성 (§6-4)
7. "다음 단계" → Flask 웹 전환 → 배포 → 기능설명서, 남은 일정과 우선순위가 명확히 정리되어 있음 (§9)

---

## 참고 산출물 (repo 내 파일)

- `scan_conditions.py`, `condition_scan.txt` — 서울 132곳 스캔
- `scan_conditions_regions.py`, `condition_scan_regions.txt` — 지역 확장 343곳 스캔
- `scan_nationwide_no_shopping.py`, `condition_scan_nationwide.txt`, `condition_scan_nationwide_raw.txt` — 전국 1,000/1,048곳 스캔 (재개 로직 포함)
- `api_helpers.py` — API 호출 공통 방어 코드 (quota 감지 등)
- `measure_judge_v3.py` — v2 대비 v3 개선폭 오프라인 측정
- `test_judge.py` — judge 회귀 테스트 20건 (실제 발견된 버그 사례 기반)
- `prototype.py` — 현재 judge v5(맹견 질문 포함) 포함 터미널 프로토타입
- `README.md` — 기술 관점 데이터 분석 메모 (이 문서는 발표 관점 정리)
